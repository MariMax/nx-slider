import { NxSliderToggleDirective } from './nx-slider-toggle.directive';

describe('NxSliderToggleDirective', () => {
  it('should create an instance', () => {
    // const directive = new NxSliderToggleDirective();
    // expect(directive).toBeTruthy();
  });
});
